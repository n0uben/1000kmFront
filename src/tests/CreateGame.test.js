const {CreateGame} = require('@/components/CreateGame.vue');
const {expect,test, describe, beforeAll, afterAll, it, jest} = require('@jest/globals');

describe('test composant create game',() => {

})